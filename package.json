{
  "name": "codecraft-academy",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.18.2",
  "scripts": {
    "dev": "vite --host",
    "generate:lessons": "node scripts/generate-static-lessons.mjs",
    "build": "pnpm generate:lessons && vite build",
    "test": "node --test tests/*.test.js",
    "validate": "pnpm test && pnpm build"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
