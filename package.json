{
  "name": "codecraft-academy",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "test": "node --test tests/*.test.js",
    "validate": "pnpm test && pnpm build"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
